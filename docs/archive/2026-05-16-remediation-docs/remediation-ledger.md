# Remediation Ledger

Updated: 2026-05-16.

Source of truth: `docs/refactor-plan.md`.

| Slice ID | Title | Priority | Findings | Status | Files changed | Tests | Verification | Result | Blocker/defer reason | Uncertainty | Last note |
|---|---|---:|---|---|---|---|---|---|---|---|---|
| R01 | Make generated-file verification fail before mutating artifacts | 1 | LC-003, DS-014 | COMPLETE | `scripts/ci-local.sh`; `tests/test_repo_guardrails.py` | Added temp-repo behavior test proving generated-clean runs before in-place build | `pytest tests/test_repo_guardrails.py`; `ruff check tests/test_repo_guardrails.py`; `bash scripts/check-generated-clean.sh`; temp-output `bash scripts/build-phyphox.sh /private/tmp/phyphox-build-check-r02` | PASS: 7 guardrail tests passed; Ruff passed; generated-clean OK; temp build produced 7 files |  | No live CI run; command behavior covered by temp script execution | Reordered `ci-local` so generated-clean runs before in-place build. |
| R02 | Make empty source builds fail | 1 | LC-004 | COMPLETE | `scripts/build-phyphox.sh`; `tests/test_repo_guardrails.py` | Added temp-repo subprocess test for missing source XML | `pytest tests/test_repo_guardrails.py`; `bash scripts/build-phyphox.sh /private/tmp/phyphox-build-check-r02`; `bash scripts/check-generated-clean.sh` | PASS: no-source behavior test passed; normal temp build still produced 7 files; generated-clean OK |  | Missing-source branch depends on `xmllint`/`python3` being present because the script checks prerequisites first | Changed no-source branch to stderr + exit 1. |
| R03 | Add astronomy XML semantic guardrails | 2 | LC-001, LC-002, DS-011 | COMPLETE | `tests/test_astronomy_semantics.py` | Added duplicate-container and unknown-reference checks across astronomy XML | `pytest tests/test_astronomy_semantics.py`; `ruff check tests/test_astronomy_semantics.py`; `bash scripts/validate-xml.sh` | PASS after R04 fix: 7 semantics tests passed; Ruff passed; XML validation OK |  | Semantic checks do not prove live phyphox runtime behavior | Guardrails now cover the confirmed duplicate-container class and unknown references. |
| R04 | Fix the confirmed tidal-locking duplicate container | 2 | LC-001, DS-011 | COMPLETE | `experiments/astronomy/tidal-locking.phyphox` | Existing and new astronomy semantic tests | `pytest tests/test_astronomy_semantics.py`; `pytest tests/test_astronomy_audit.py tests/test_astronomy_consolidation.py`; `bash scripts/validate-xml.sh` | PASS: semantics 7 passed; audit/consolidation 14 passed; XML validation OK |  | Manual phyphox import not run | Removed extra first-block `factor2` container; existing `factor1` and second-block `factor2` remain. |
| R05 | Tighten core mode and payload-offset validation | 2 | LC-005, LC-006, DS-012 | COMPLETE | `tools/validate_phyphox.py`; `tests/test_phyphox_validate.py` | Added mode-value and duplicate-offset regression tests | `pytest tests/test_phyphox_validate.py tests/test_phyphox_file_contracts.py`; `ruff check tools/validate_phyphox.py tests/test_phyphox_validate.py`; `bash scripts/validate-xml.sh` | PASS: 82 tests passed; Ruff passed; XML validation OK |  | No live app import; validator contract is static | Mode configs must be finite active integer IDs; duplicate payload offsets now fail validation. |
| R06 | Replace brittle guardrail tests with behavior checks | 4 | LC-008, DS-013, DS-014, DS-019 | COMPLETE | `tests/test_repo_guardrails.py` | Removed leftover literal workflow/script text assertion after adding temp-repo behavior coverage | `pytest tests/test_repo_guardrails.py tests/test_phyphox_validate.py tests/test_astronomy_semantics.py`; `ruff check tests/test_repo_guardrails.py tests/test_phyphox_validate.py tests/test_astronomy_semantics.py` | PASS: 73 tests passed; Ruff passed |  | Some private-helper tests remain where they still support namespace helper behavior; not all test-quality concerns are deleted | Replaced the brittle guardrail assertion without broad test churn. |
| R07 | Align firmware initial config state with default mode | 2 | LC-010 | COMPLETE | `arduino/phyphox_ble_sense/phyphox_ble_sense.ino`; `tests/test_repo_guardrails.py` | Added static firmware contract test | `pytest tests/test_repo_guardrails.py`; `ruff check tests/test_repo_guardrails.py`; `bash scripts/validate-xml.sh`; `bash scripts/compile-arduino.sh` | PASS: 8 guardrail tests passed; Ruff passed; XML validation OK; Arduino compile passed after sandbox escalation |  | Live BLE readback not run | Initial config characteristic now encodes acceleration, matching firmware default mode. |
| R08 | Define and enforce reserved-mode behavior | 2 | LC-011, DS-006 | COMPLETE | `arduino/phyphox_ble_sense/phyphox_ble_sense.ino`; `arduino/phyphox_ble_sense/README.md`; `README.md`; `docs/architecture-map.md`; `tests/test_repo_guardrails.py` | Added static firmware contract test | `pytest tests/test_repo_guardrails.py`; `ruff check tests/test_repo_guardrails.py`; `bash scripts/validate-xml.sh`; `bash scripts/compile-arduino.sh`; `rg -n 'Rounded to the nearest\|mode \\(1–9\\)\|mode \\(1-9\\)\|1–9\|accepts only raw values `1\\.\\.9`\|rounds config\|full range `1\\.\\.9`' README.md docs arduino/phyphox_ble_sense/README.md`; `git diff --check` | PASS: 8 guardrail tests passed; Ruff passed; XML validation OK; Arduino compile passed after sandbox escalation; current docs no longer describe active config as `1-9` or rounded; diff whitespace check passed |  | Live reserved-mode BLE write probe not run; original audit finding still contains historical `1..9` evidence | Firmware now rejects fractional config values, no longer accepts a broad reserved range, and current docs describe active modes as `1`-`6` or `9`. |
| R09 | Surface unavailable sensor modes instead of streaming ambiguous NaNs | 2 | LC-009 | BLOCKED | `README.md`; `docs/RUNBOOK.md` | None; documentation-only clarification of existing firmware README contract | Static inspection; `nl -ba arduino/phyphox_ble_sense/phyphox_ble_sense.ino | sed -n '1,260p'`; `cat experiments/phyphox_constants.json`; `arduino-cli board list`; `ls /dev/cu.*`; `rg -n "NaN\|unavailable sensor\|unavailable-sensor\|stale sample\|No data / flat plot" README.md docs/RUNBOOK.md arduino/phyphox_ble_sense/README.md docs/remediation-ledger.md docs/remediation-status.md`; `git diff --check` | PARTIAL: existing firmware README defines five-float `NaN` semantics; README/RUNBOOK now expose that unavailable/stale samples must not be treated as real zero; no Arduino Nano 33 BLE Sense is detected, only unknown `/dev/cu.Bluetooth-Incoming-Port` and `/dev/cu.debug-console` ports; diff whitespace check passed | Live phyphox/hardware probe is still unavailable, so the repo cannot prove the app surfaces `NaN` clearly enough to satisfy the R09 Definition of Done. Changing the protocol without that evidence could replace one false-success mode with another. | Needs live BLE/phyphox evidence for the documented `NaN` unavailable-sensor contract, or an explicit owner decision to replace that contract | User-facing docs now state the current `NaN` unavailable-sensor contract; code/protocol completion remains externally blocked. |
| R10 | Add guards for zero and incomplete astronomy calculations | 3 | LC-007, LC-012, DS-010 | DEFERRED | None | None | Static inspection only | Not run | Requires live phyphox expression/runtime verification for zero, NaN, empty, and not-ready display behavior. Editing formulas without that evidence risks replacing one silent wrong result with another. | Needs phyphox app import/simulated data evidence | Deferred until runtime semantics are verified. |
| R11 | Clarify auxiliary and stale documentation surfaces | 5 | DS-002, DS-003, DS-005, DS-015, DS-020 | COMPLETE | `README.md`; `docs/REPO_MAP.md`; `docs/ci/README.md`; `docs/ci/ci.md`; `docs/deprecated/audit/README.md` | Existing astronomy docs/semantics tests | `rg -n "agent\\.md\|docs/ci\|ci-decision\|deprecated/audit\|owon\|Owon\|debug" README.md docs .gitignore`; `pytest tests/test_astronomy_audit.py tests/test_astronomy_consolidation.py tests/test_astronomy_semantics.py`; `ruff check .` | PASS: targeted docs search showed no stale current `agent.md` reference outside audits/indexes; 21 astronomy tests passed; Ruff passed |  | Archive/audit docs still intentionally mention historical paths and findings | Current docs now route CI through `docs/ci.md`, mark deprecated audit content as archive-only, and describe Owon debug material as auxiliary. |
| R12 | Decide the tracked Dependabot config state | 5 | DS-001 | COMPLETE | `.github/dependabot.yml` remains deleted, matching `origin/main` | None | `git status --short`; `git log --oneline -- .github/dependabot.yml`; `git show HEAD:.github/dependabot.yml`; `rg -n "dependabot\|Dependency\|dependencies" .github docs README.md pyproject.toml requirements-test.txt`; `git status --short --branch`; `gh api repos/sebastianspicker/phyphox-arduino-classroom-kit/contents/.github/dependabot.yml`; `git ls-tree -r origin/main -- .github/dependabot.yml`; `git log --oneline HEAD..origin/main`; `git show --name-status --oneline origin/main -- .github/dependabot.yml` | PASS: local branch is behind `origin/main` by `4fd276c chore: disable dependabot updates`; that remote default-branch commit deletes `.github/dependabot.yml`, and the GitHub contents API returns 404 for the file |  | Local checkout is behind remote by one commit, so the deletion is represented as a local worktree deletion until branch state is reconciled | Treated the Dependabot removal as intentional remote-default-branch state, not an accidental local deletion. |
| R13 | Simplify build/security script internals without changing behavior | 6 | DS-016, DS-017, DS-018 | COMPLETE | `scripts/deps-scan.sh`; `scripts/secret-scan.sh` | Existing guardrail test | `bash scripts/sast-minimal.sh`; `shellcheck scripts/*.sh`; `bash scripts/validate-xml.sh`; `bash scripts/check-generated-clean.sh`; `bash scripts/deps-scan.sh`; `bash scripts/secret-scan.sh`; `bash scripts/build-phyphox.sh /private/tmp/phyphox-build-check-r13`; `pytest tests/test_repo_guardrails.py`; `ruff check .` | PASS after one corrected Bash-regex edit: shell syntax/ShellCheck/py_compile OK; XML validation OK; generated-clean OK; dependency scan OK; secret scan OK; temp build produced 7 files; guardrails 8 passed; Ruff passed |  | Did not replace custom secret scanning or dependency scanning with new tooling; scope stayed behavior-preserving | Removed duplicate temp cleanup trap and replaced one pipeline with a Bash regex variable while preserving command outputs. |
| R14 | Simplify firmware mode dispatch only after runtime behavior is covered | 6 | DS-007, DS-008, LC-009, LC-011 | BLOCKED | None | None | `sed -n '376,401p' docs/refactor-plan.md`; `rg -n "void readChannels\|switch\|currentMode\|Mode::\|read[A-Z]\|payload" arduino/phyphox_ble_sense/phyphox_ble_sense.ino`; `arduino-cli board list`; `ls /dev/cu.*` | INSPECTED: `readChannels()` remains a long active-mode branch in a runtime-critical loop; no Arduino Nano 33 BLE Sense is detected for live active-mode probes | R14 explicitly depends on higher-risk behavior coverage from R09 plus live BLE probes for all active modes. R09 is blocked, and no live BLE/phyphox evidence is available. | Without live mode probes, a style refactor could preserve compile success while changing payload timing, bytes, or unavailable-sensor behavior | Blocked until unavailable-mode semantics and live active-mode probes exist. |
| R15 | Evaluate XML deduplication only where generation already exists | 6 | DS-009, DS-010, DS-011 | DEFERRED | None | None | `sed -n '403,423p' docs/refactor-plan.md`; `find src/phyphox experiments/astronomy -maxdepth 2 -type f`; `rg -n "xi:include\|include\|container\|dataInput\|dataOutput" src/phyphox experiments/astronomy \| head -n 120` | INSPECTED: generated core XML already uses shared XInclude snippets; astronomy XML remains hand-edited and runtime-sensitive | Additional deduplication would be mostly maintainability churn, and the plan explicitly warns astronomy deduplication is high risk without phyphox import/runtime proof. No narrow behavior-backed cleanup was identified beyond R04's confirmed duplicate-container fix. | Needs phyphox import spot-checks and a concrete repeated source pattern that can be mechanically preserved | Deferred until a specific generated-source deduplication candidate has runtime/import verification. |
